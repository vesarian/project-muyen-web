{
  "name": "muyen",
  "version": "1.0.0",
  "description": "Project Website Muyen Coffee",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "sequelize db:drop && sequelize db:create && sequelize db:migrate && sequelize db:seed:all",
    "dbtest": "sequelize --env test db:drop && sequelize --env test db:create && sequelize --env test db:migrate && sequelize --env test db:seed:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.8.0",
    "sequelize": "^6.27.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "jest": "^29.3.1",
    "sequelize-cli": "^6.5.2",
    "supertest": "^6.3.3"
  }
}
